elements-dir: /etc/nodepool/elements
images-dir: /var/lib/nodepool/dib

cron:
  cleanup: '*/30 * * * *'
  check: '*/15 * * * *'

zmq-publishers: []

zookeeper-servers:
  - host: localhost
    port: 2181

gearman-servers:
  - host: localhost

labels: []

providers: []

targets: []

diskimages: []
